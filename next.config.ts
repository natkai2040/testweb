/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // output: 'export',
  images: { unoptimized: false },
  
  // basePath: "/testweb",
}

module.exports = nextConfig;
